# 标书工坊 · 前端

Vite + React + TypeScript。按 feature 拆分，对齐易标 C 端模块。

## 命令

```bash
npm install
npm run dev
npm run build
```

## 目录

见仓库根目录 `docs/CONTRIBUTING.md`。

## 环境变量

复制 `.env.example` 为 `.env` 后按需修改 `VITE_API_BASE_URL`。
