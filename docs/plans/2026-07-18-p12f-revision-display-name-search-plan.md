# P12F-I 修订名称与可见内容联合搜索实施计划

> **执行者：Grok**：严格四文件；先只写后端 P12F-I 测试与 history E2E 形成真实红测，再实现后端联合匹配和共用面板文案；只自测并通过消息箱请求审查，不暂存、不提交、不推送。
>
> **状态：** 2026-07-19 已完成。冻结=`060191e`，实现=`008e443`，Grok review_request=`msg_82cd1e26df03413389a92604830cdb9c`，Codex 验收回执=`msg_d954063f489248babb027b9bb335f666`。

**目标：** 让现有显式搜索同时命中可选修订展示名称和既有可见快照字段，不改变接口、候选范围、筛选、严格校验或返回形状。

**架构：** search SQL 已投影并验证 `display_name`；在完整候选验证后，用同一 NFKC+casefold 规则计算名称命中，与既有快照白名单命中做显式联合。前端不改 API，只修正共用入口及固定状态文案。

## 1. 基线与 failure-first

1. 核对分支为 `collab/grok-code-codex-review`，HEAD/远端为冻结提交，工作区干净、暂存区为空；逐字核对四文件哈希。
2. 第一阶段只改两个测试文件。后端 P12F-I 用例必须先以合法 ASGI/数据库夹具证明名称唯一命中缺失；前端必须先进入已加载页面后因联合搜索标签缺失失败。
3. 同时建立强证据：名称+内容双命中只返一次且倒序；NFKC/casefold；null/非命中；第 20/21 条边界；名称命中仍不短路坏快照；来源/时间/空间组合；响应六键与五域零写。
4. 记录 failed/passed/did-not-run、首个业务失败、两生产文件哈希与精确两测试文件状态；无效环境失败不得进入实现阶段。

## 2. 后端实现

1. 更新 history service 顶部/函数注释为“名称与可见内容联合搜索”，删除“名称不参与匹配”的旧事实。
2. 复用 `_validate_stored_display_name` 的结果；新增或内联一个名称匹配 helper，对非 null 名称调用既有 `_fold_for_search`，精确连续包含查询针。
3. `list_editor_state_revision_search` 仍先把全部候选验证为 `(meta, snapshot)`；第二阶段按 `name_match or snapshot_match` 追加 `meta` 一次。禁止在验证循环中早返、按名称跳过快照验证或重复追加。
4. SQL、候选 20、七列顺序、错误优先级、六键返回、`no-store` 与所有写域保持字节语义不变。

## 3. 前端实现

1. 共用面板文件顶注释加入 P12F-I；固定文案改为“修订名称或内容搜索失败，请稍后重试”“没有匹配名称或内容的修订”。
2. 标签精确“名称或内容搜索”，活动态精确“当前为名称或内容搜索结果”；不得更改输入规则、请求函数、状态机、重载或互斥逻辑。
3. history E2E 增加 P12F-I 独立技术/商务用例与终态静态门，证明联合标签、名称结果文本安全、精确 POST、组合筛选、失败/空态、迟到与零泄漏；既有 P12F-F-B 用例同步准确文案但不得放宽。

## 4. 受限审查

1. Codex 比较四文件最终哈希与冻结值，确认无第五文件、无暂存、无路由/Schema/API/模型/迁移变化。
2. 后端逐行确认先验后搜、同一折叠规则、null 分支、并集去重、20 条窗、坏候选不短路、七列 SQL 和零写。
3. 前端确认只有事实注释/固定文案变化，搜索状态机和所有安全围栏未被重写；E2E 禁止客户端过滤、空数组真值、route fallback、宽计数或固定 sleep。
4. 若发现问题，只能在四文件内下发最小返修；任何确需扩围必须先由 Codex 修订契约并提交推送，Grok 不得自行增加文件。

## 5. 串行验收与交付

按契约第 6 节严格逐条运行。Grok 完成后发送 `review_request`，报告红测、精确四文件、测试数字/耗时、SQL/匹配/联合/先验后搜/文本安全/泄漏证据、哈希、风险与未做项。

Codex 独立审查、重跑后端专项/兼容/全量和前端聚焦/history/checkpoint/truth/lint/build/全量；全部通过后才以中文提交实现、推送、更新契约/计划/交接/路线图/联调清单并发送验收回执。

完成事实：Grok 真实 failure-first 为后端 **5 failed / 1 passed**、前端 **2 failed / 1 passed**；实现后严格四文件、暂存区空，未执行 Git。Codex 静态审查确认先验后搜、名称/内容并集去重、20/21 边界、七列 SQL 和前端只改固定文案；独立串行通过后端 **29/247/1146 passed**、前端 **3/55/51/28/18/318 passed**，lint/build/py_compile/diff-check/哈希/静态门全部通过。中文实现提交 `008e443` 已推送协作分支。

## 6. 未做

固定/置顶及裁剪保护、名称编辑、批量/标签、搜索高亮/片段/评分/游标/缓存、自动搜索、跨项目搜索/历史、检查点命名、导出/分享、多人协作、SSE、索引/迁移均不进入 P12F-I。
